plugins {
    id "com.android.library"
    id "com.jakewharton.butterknife"
}

// in config.gradle
configAndroidDomain project

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    compileOnly project(':fly')
}

apply from: 'bintray-http.gradle'
